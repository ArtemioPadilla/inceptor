---
description: Run the preflight check for this scaffold (node version, gh auth, clean tree, branch naming).
---

Run `npm run doctor` and surface the result.

```bash
npm run doctor
```

Report which checks passed, which failed, and the exact remediation step for
each failure. Do not attempt to fix anything yourself — preflight is
diagnostic only; the user decides which fixes to apply.
