{
  "name": "inceptor-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-consumable MCP server exposing Inceptor's component registry (Epic 26). Sibling dev-tool package — not a request-serving backend archetype like server-node/server-flask (ADR 0006).",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}
