# Environment secrets — never commit real secrets
.env
.env.*
# …but the committed example file must NOT be ignored (it carries no secrets)
!.env.example

# Build outputs
node_modules/
dist/
.astro/
.cache/

# Logs (any .log file — including firebase-debug.log, npm-debug.log, etc.)
*.log
logs/

# Test / CI artifacts
test-results/
playwright-report/
.lighthouseci/
coverage/

# vibe-test harness scratch dir (scripts/vibe-test/score.ts) — per-run
# temp files it always cleans up itself, but ignore the parent in case a
# run is killed mid-flight
.vibe-test-tmp/

# OS / editor artifacts
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/settings.json

# Stray local files that sneaked into the working tree during development
image002.png
names.md
